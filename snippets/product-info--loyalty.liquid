
<div class="product-info--loyalty">

{%- render 'icon' with 'go-to-peach' -%}

{%- if customer -%}

    {%- for tag in customer.tags -%}
        {%- if tag contains "Rivo VIP Tier: Peach Friend" -%}
            {%- assign peachFriendCustomer = true -%}{% break %}
        {%- endif -%}
    {%- endfor -%}

    <div class="product-info--loyalty-text">
    {%- if peachFriendCustomer -%}
        Earn {{ product.price | times: 2 | divided_by: 100 }} Points! <br><span class="small">Plus 50 Points On Recurring Subscriptions!</span>
    {% else  %} 
        Earn {{ product.price | divided_by: 100 }} Points! <br><span class="small">Plus 50 Points On Recurring Subscriptions!</span>
    {%- endif -%}
    </div>

{% else %}
    {% comment %} <a href="{{ routes.account_register_url }}"></a>Signup or <a href="{{ routes.account_login_url }}">Login</a> Earn earn {{ product.price | divided_by: 100 }} Points {% endcomment %}
      Earn {{ product.price | divided_by: 100 }} Points  <a href="https://gotoskincare.com/pages/go-to-gang">learn more</a>

{%- endif -%}
</div>